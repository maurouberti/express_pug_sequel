mixin form(event)
    div(class="form-group")
        input(type="text", class="form-control", placeholder="Nome", name="name", value=event.name)
    div(class="form-group")
       textarea(class="form-control", placeholder="Descrição", name="description")=event.description
    div(class="form-group")
        button(type="submit", class="btn btn-success") Gravar
